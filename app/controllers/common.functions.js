
/*mapDataDocuments = (data) => {
    var newData = data.map(function(obj){
        var nObj = {};
        nObj.id = obj.id;
        nObj.name = obj.name;
        nObj.abstract = obj.abstract;
        nObj.category = obj.category;
        return nObj;
    });

    return newData;
}

module.exports = { mapDataDocuments };*/